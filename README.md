1. Заменила часть рассылки в файле news_portal/tasks.py
Проблемы:
Отправка писем выполнялась последовательно — каждое письмо ждёт завершения предыдущего.
При 100+ подписчиках задержка огромная.
Нельзя контролировать число потоков или логгировать состояние по каждой отправке.
Исключения (ошибки SMTP) не логгировались централизованно.
2. В начало news_portal/tasks.py (после импортов) добавлен новый хелпер-функция, чтобы не дублировать код отправки
3. Новый параметр в настройках (можно добавить в settings.py).Чтобы удобно регулировать число потоков без правки кода
4. Новый модуль news_portal/utils_parallel.py. Пример для CPU-bound задач (многопроцессорность)
